""" Practice file for linked_list.md tutorial """
""" 
class Node:
            
            def _init_(value, head = self, tail = self, next = None, prev =  None):
                self.value = value
                self.head = head
                self.tail = tail
                self.next = next
                self.prev = prev

#Create head - you can define default values will take care of the rest 
head_node = Node("Howdy") """

print("Hello")